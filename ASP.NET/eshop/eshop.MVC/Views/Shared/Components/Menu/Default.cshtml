@model IEnumerable<Category>

<div class="list-group">
    @foreach (var category in Model)
    {
        <a asp-action="Index" asp-controller="Home" asp-route-categoryId="@category.Id" asp-route-page="" class="list-group-item list-group-item-action">@category.Name</a>
    }
   
</div>